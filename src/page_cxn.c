/* AUTO-GENERATED by tools/cxnc — do not edit */
#include "pages.h"

static void render(HttpRequest *req, PageCtx *ctx) {
    (void)req;
    page_write(ctx, "<div class=\"docs-layout\">\n");
    page_write(ctx, "  <aside class=\"docs-sidebar\">\n");
    page_write(ctx, "    <div class=\"docs-nav-group\">Getting Started</div>\n");
    page_write(ctx, "    <a href=\"#intro\"      class=\"docs-nav-link\">Introduction</a>\n");
    page_write(ctx, "    <a href=\"#quickstart\" class=\"docs-nav-link\">Quick Start</a>\n");
    page_write(ctx, "    <div class=\"docs-nav-group\">Routing</div>\n");
    page_write(ctx, "    <a href=\"#routing\"    class=\"docs-nav-link\">File Routing</a>\n");
    page_write(ctx, "    <a href=\"#layout\"     class=\"docs-nav-link\">Layout</a>\n");
    page_write(ctx, "    <div class=\"docs-nav-group\">Pages</div>\n");
    page_write(ctx, "    <a href=\"#templates\"  class=\"docs-nav-link\">.cxn Templates</a>\n");
    page_write(ctx, "    <a href=\"#actions\"    class=\"docs-nav-link\">Server Actions</a>\n");
    page_write(ctx, "    <a href=\"#apiroutes\"  class=\"docs-nav-link\">API Routes</a>\n");
    page_write(ctx, "    <div class=\"docs-nav-group\">Data</div>\n");
    page_write(ctx, "    <a href=\"#database\"   class=\"docs-nav-link\">Database</a>\n");
    page_write(ctx, "    <a href=\"#fetch\"      class=\"docs-nav-link\">HTTP Fetch</a>\n");
    page_write(ctx, "  </aside>\n");
    page_write(ctx, "  <div class=\"docs-content\">\n");
    page_write(ctx, "    <div class=\"docs-section\" id=\"intro\">\n");
    page_write(ctx, "      <h1 class=\"docs-title\">cnext</h1>\n");
    page_write(ctx, "      <p class=\"docs-lead\">Next.js-style SSR framework written in C. File-based routing, <code>.cxn</code> templates, server actions &mdash; compiled to a single binary powered by <code>io_uring</code> + HTTP/2 + LMDB.</p>\n");
    page_write(ctx, "      <div class=\"tech-grid\">\n");
    page_write(ctx, "        <div class=\"tech-card\">\n");
    page_write(ctx, "          <div class=\"tech-icon\">&#9889;</div>\n");
    page_write(ctx, "          <div class=\"tech-name\">io_uring</div>\n");
    page_write(ctx, "          <div class=\"tech-desc\">Multishot accept &bull; provided buffers &bull; SQPOLL &bull; zero-copy send</div>\n");
    page_write(ctx, "        </div>\n");
    page_write(ctx, "        <div class=\"tech-card\">\n");
    page_write(ctx, "          <div class=\"tech-icon\">&#128228;</div>\n");
    page_write(ctx, "          <div class=\"tech-name\">HTTP/2</div>\n");
    page_write(ctx, "          <div class=\"tech-desc\">nghttp2 &bull; HPACK &bull; stream multiplexing</div>\n");
    page_write(ctx, "        </div>\n");
    page_write(ctx, "        <div class=\"tech-card\">\n");
    page_write(ctx, "          <div class=\"tech-icon\">&#128190;</div>\n");
    page_write(ctx, "          <div class=\"tech-name\">LMDB</div>\n");
    page_write(ctx, "          <div class=\"tech-desc\">Async write queue &bull; batch commit &bull; MDB_MAPASYNC</div>\n");
    page_write(ctx, "        </div>\n");
    page_write(ctx, "        <div class=\"tech-card\">\n");
    page_write(ctx, "          <div class=\"tech-icon\">&#128204;</div>\n");
    page_write(ctx, "          <div class=\"tech-name\">Hot Cache</div>\n");
    page_write(ctx, "          <div class=\"tech-desc\">RAM hash table &bull; FNV-1a &bull; LRU &bull; background eviction</div>\n");
    page_write(ctx, "        </div>\n");
    page_write(ctx, "      </div>\n");
    page_write(ctx, "    </div>\n");
    page_write(ctx, "    <div class=\"docs-section\" id=\"quickstart\">\n");
    page_write(ctx, "      <h2 class=\"docs-h2\">Quick Start</h2>\n");
    page_write(ctx, "      <p class=\"docs-p\">Build and run with Docker. io_uring is Linux-only, so macOS users must always use Docker.</p>\n");
    page_write(ctx, "      <div class=\"code-block\">docker build -t cnext .\n");
    page_write(ctx, "docker run --rm -p 8080:8080       \\\n");
    page_write(ctx, "  --security-opt seccomp=unconfined \\\n");
    page_write(ctx, "  --cap-add SYS_NICE                \\\n");
    page_write(ctx, "  --ulimit nofile=65536:65536       \\\n");
    page_write(ctx, "  cnext</div>\n");
    page_write(ctx, "      <p class=\"docs-note\">Mount <code>-v $(pwd)/data:/app/data</code> to persist the LMDB database across container restarts.</p>\n");
    page_write(ctx, "    </div>\n");
    page_write(ctx, "    <div class=\"docs-section\" id=\"routing\">\n");
    page_write(ctx, "      <h2 class=\"docs-h2\">File-based Routing</h2>\n");
    page_write(ctx, "      <p class=\"docs-p\">Place files under <code>src/</code> and routes are derived from the folder path &mdash; no <code>@route</code> needed, no wiring in <code>main.c</code>.</p>\n");
    page_write(ctx, "      <table class=\"ref-table\">\n");
    page_write(ctx, "        <tr><th>File</th><th>Method</th><th>Route</th></tr>\n");
    page_write(ctx, "        <tr><td><code>src/page.cxn</code></td><td class=\"method\">GET</td><td><code>/</code></td></tr>\n");
    page_write(ctx, "        <tr><td><code>src/blog/page.cxn</code></td><td class=\"method\">GET</td><td><code>/blog</code></td></tr>\n");
    page_write(ctx, "        <tr><td><code>src/blog/route.c</code></td><td class=\"method\">GET &bull; POST</td><td><code>/blog</code></td></tr>\n");
    page_write(ctx, "        <tr><td><code>src/user/action.c</code></td><td class=\"method\">POST</td><td><code>/action/user/create</code></td></tr>\n");
    page_write(ctx, "        <tr><td><code>src/user/action_delete.c</code></td><td class=\"method\">POST</td><td><code>/action/user/delete</code></td></tr>\n");
    page_write(ctx, "        <tr><td><code>src/layout.cxn</code></td><td class=\"dim\">layout</td><td>wraps every page</td></tr>\n");
    page_write(ctx, "      </table>\n");
    page_write(ctx, "    </div>\n");
    page_write(ctx, "    <div class=\"docs-section\" id=\"layout\">\n");
    page_write(ctx, "      <h2 class=\"docs-h2\">Layout</h2>\n");
    page_write(ctx, "      <p class=\"docs-p\">Create <code>src/layout.cxn</code> with a <code>&#123;&#123;children&#125;&#125;</code> marker. Every page is rendered at that slot &mdash; no explicit calls in the page.</p>\n");
    page_write(ctx, "      <div class=\"code-block\">&lt;!DOCTYPE html&gt;\n");
    page_write(ctx, "&lt;html lang=\"en\"&gt;\n");
    page_write(ctx, "&lt;head&gt;\n");
    page_write(ctx, "  &lt;title&gt;cnext&lt;/title&gt;\n");
    page_write(ctx, "  &lt;link rel=\"stylesheet\" href=\"/style.css\"&gt;\n");
    page_write(ctx, "&lt;/head&gt;\n");
    page_write(ctx, "&lt;body&gt;\n");
    page_write(ctx, "  &lt;nav class=\"navbar\"&gt;...&lt;/nav&gt;\n");
    page_write(ctx, "  &lt;main&gt;\n");
    page_write(ctx, "    &#123;&#123;children&#125;&#125;\n");
    page_write(ctx, "  &lt;/main&gt;\n");
    page_write(ctx, "&lt;/body&gt;\n");
    page_write(ctx, "&lt;/html&gt;</div>\n");
    page_write(ctx, "      <p class=\"docs-p\">For shared partials outside the root layout, use <code>&#64;fn funcname</code> to emit a public helper instead of a page.</p>\n");
    page_write(ctx, "    </div>\n");
    page_write(ctx, "    <div class=\"docs-section\" id=\"templates\">\n");
    page_write(ctx, "      <h2 class=\"docs-h2\">.cxn Templates</h2>\n");
    page_write(ctx, "      <p class=\"docs-p\">Files are compiled at build time by <code>tools/cxnc</code> (Python). Each <code>.cxn</code> becomes a <code>_cxn.c</code> file that is compiled into the binary — zero runtime parsing overhead.</p>\n");
    page_write(ctx, "      <div class=\"code-block\">&lt;!-- src/users/page.cxn  →  GET /users  (route derived from path) --&gt;\n");
    page_write(ctx, "&lt;%inc db/db.h %&gt;\n");
    page_write(ctx, "&lt;%!\n");
    page_write(ctx, "static void card(const char *k, const char *v, void *arg) {\n");
    page_write(ctx, "    PageCtx *ctx = arg;\n");
    page_write(ctx, "    page_writef(ctx, \"&lt;div class='card'&gt;%s&lt;/div&gt;\", v);\n");
    page_write(ctx, "}\n");
    page_write(ctx, "%&gt;\n");
    page_write(ctx, "&lt;section class='page'&gt;\n");
    page_write(ctx, "  &lt;% db_scan(\"users\", card, ctx); %&gt;\n");
    page_write(ctx, "&lt;/section&gt;</div>\n");
    page_write(ctx, "      <table class=\"ref-table\">\n");
    page_write(ctx, "        <tr><th>Directive</th><th>Description</th></tr>\n");
    page_write(ctx, "        <tr><td><code>&lt;%inc file.h %&gt;</code></td><td><code>#include \"file.h\"</code></td></tr>\n");
    page_write(ctx, "        <tr><td><code>&lt;%inc &lt;stdlib.h&gt; %&gt;</code></td><td><code>#include &lt;stdlib.h&gt;</code></td></tr>\n");
    page_write(ctx, "        <tr><td><code>&#64;route /path</code></td><td>Override file-path-derived route (optional)</td></tr>\n");
    page_write(ctx, "        <tr><td><code>&#64;fn funcname</code></td><td>Public <code>void funcname(PageCtx *ctx)</code>, no route</td></tr>\n");
    page_write(ctx, "        <tr><td><code>&lt;%! ... %&gt;</code></td><td>C code at file scope (close with <code>%&gt;</code> on its own line)</td></tr>\n");
    page_write(ctx, "        <tr><td><code>&lt;% code; %&gt;</code></td><td>C code inside <code>render()</code></td></tr>\n");
    page_write(ctx, "        <tr><td><code>&#123;&#123;expr&#125;&#125;</code></td><td><code>page_writef(ctx, \"%s\", expr)</code></td></tr>\n");
    page_write(ctx, "        <tr><td>plain HTML</td><td><code>page_write(ctx, \"...\\n\")</code></td></tr>\n");
    page_write(ctx, "      </table>\n");
    page_write(ctx, "    </div>\n");
    page_write(ctx, "    <div class=\"docs-section\" id=\"actions\">\n");
    page_write(ctx, "      <h2 class=\"docs-h2\">Server Actions</h2>\n");
    page_write(ctx, "      <p class=\"docs-p\">Create <code>src/path/action.c</code> — a POST handler that processes form data and redirects. Multiple actions per directory use separate files (<code>action.c</code>, <code>action_delete.c</code>, &hellip;) since each defines <code>static void action</code>.</p>\n");
    page_write(ctx, "      <div class=\"code-block\">#include \"route.h\"\n");
    page_write(ctx, "#include \"server.h\"\n");
    page_write(ctx, "#include \"db/db.h\"\n");
    page_write(ctx, "#include &lt;string.h&gt;\n");
    page_write(ctx, "static void action(HttpRequest *req, int socket_fd) {\n");
    page_write(ctx, "    char name[256] = \"\";\n");
    page_write(ctx, "    parse_form(req-&gt;body, \"name\", name, sizeof(name));\n");
    page_write(ctx, "    if (!name[0]) {\n");
    page_write(ctx, "        send_redirect(socket_fd, \"/path?error=empty\");\n");
    page_write(ctx, "        return;\n");
    page_write(ctx, "    }\n");
    page_write(ctx, "    db_put(DB_BOTH, \"items\", name, name);\n");
    page_write(ctx, "    send_redirect(socket_fd, \"/path\");\n");
    page_write(ctx, "}\n");
    page_write(ctx, "REGISTER_ACTION(\"/action/path/create\")</div>\n");
    page_write(ctx, "      <table class=\"ref-table\">\n");
    page_write(ctx, "        <tr><th>Helper</th><th>Description</th></tr>\n");
    page_write(ctx, "        <tr><td><code>parse_form(body, key, out, len)</code></td><td>Parse <code>application/x-www-form-urlencoded</code>, URL-decode into <code>out</code></td></tr>\n");
    page_write(ctx, "        <tr><td><code>send_redirect(fd, location)</code></td><td>Send HTTP 302 to <code>location</code></td></tr>\n");
    page_write(ctx, "      </table>\n");
    page_write(ctx, "    </div>\n");
    page_write(ctx, "    <div class=\"docs-section\" id=\"apiroutes\">\n");
    page_write(ctx, "      <h2 class=\"docs-h2\">API Routes</h2>\n");
    page_write(ctx, "      <p class=\"docs-p\">Create <code>src/path/route.c</code> for JSON endpoints. Use <code>REGISTER_ROUTE</code> to handle all methods, or <code>REGISTER_GET</code> / <code>REGISTER_POST</code> for specific ones.</p>\n");
    page_write(ctx, "      <div class=\"code-block\">#include \"route.h\"\n");
    page_write(ctx, "#include \"server.h\"\n");
    page_write(ctx, "#include \"db/db.h\"\n");
    page_write(ctx, "static void handle(HttpRequest *req, int socket_fd) {\n");
    page_write(ctx, "    if (strcmp(req-&gt;method, \"GET\") == 0) {\n");
    page_write(ctx, "        char *list = db_list(\"items\");\n");
    page_write(ctx, "        send_response(socket_fd, 200, \"OK\", \"application/json\", list);\n");
    page_write(ctx, "        free(list);\n");
    page_write(ctx, "    }\n");
    page_write(ctx, "}\n");
    page_write(ctx, "REGISTER_ROUTE(\"/api/items\")</div>\n");
    page_write(ctx, "    </div>\n");
    page_write(ctx, "    <div class=\"docs-section\" id=\"database\">\n");
    page_write(ctx, "      <h2 class=\"docs-h2\">Database</h2>\n");
    page_write(ctx, "      <p class=\"docs-p\">Two-tier storage: <strong>hot</strong> (RAM hash table, nanosecond reads) and <strong>cold</strong> (LMDB on disk, async writes). Use <code>DB_BOTH</code> to write to both at once.</p>\n");
    page_write(ctx, "      <div class=\"code-block\">#include \"db/db.h\"\n");
    page_write(ctx, "db_put(DB_BOTH, \"ns\", \"key\", \"value\"); // hot + cold\n");
    page_write(ctx, "db_put(DB_HOT,  \"ns\", \"key\", \"value\"); // RAM only, not persistent\n");
    page_write(ctx, "db_put(DB_COLD, \"ns\", \"key\", \"value\"); // LMDB only, async\n");
    page_write(ctx, "char *val  = db_get(\"ns\", \"key\");      // hot first, then cold; free() after use\n");
    page_write(ctx, "db_del(\"ns\", \"key\");                   // both tiers\n");
    page_write(ctx, "db_scan(\"ns\", callback, ctx);          // iterate all values\n");
    page_write(ctx, "char *list = db_list(\"ns\");            // returns JSON \"[v1,v2,...]\"; free() after use</div>\n");
    page_write(ctx, "      <p class=\"docs-note\">Cold writes are async. <code>db_scan</code> and <code>db_list</code> call <code>cold_flush()</code> internally — they always read committed data.</p>\n");
    page_write(ctx, "    </div>\n");
    page_write(ctx, "    <div class=\"docs-section\" id=\"fetch\">\n");
    page_write(ctx, "      <h2 class=\"docs-h2\">HTTP Fetch</h2>\n");
    page_write(ctx, "      <p class=\"docs-p\">Use <code>cxn_fetch()</code> for server-side HTTP requests via libcurl. It is blocking — call it from a background thread to avoid stalling io_uring workers.</p>\n");
    page_write(ctx, "      <div class=\"code-block\">#include \"fetch.h\"\n");
    page_write(ctx, "FetchResult *fr = cxn_fetch(\"https://api.example.com/data\", \"GET\", NULL);\n");
    page_write(ctx, "if (fr) {\n");
    page_write(ctx, "    // fr-&gt;body    null-terminated response body\n");
    page_write(ctx, "    // fr-&gt;len     byte length\n");
    page_write(ctx, "    // fr-&gt;status  HTTP status code (200, 404, …)\n");
    page_write(ctx, "    fetch_free(fr);\n");
    page_write(ctx, "}\n");
    page_write(ctx, "// POST with a body\n");
    page_write(ctx, "FetchResult *fr2 = cxn_fetch(\"https://api.example.com/submit\", \"POST\", \"key=value\");</div>\n");
    page_write(ctx, "    </div>\n");
    page_write(ctx, "  </div>\n");
    page_write(ctx, "</div>\n");
}

__attribute__((constructor))
static void _cxn_page_ctor(void) { add_page("GET", "/", render); }

